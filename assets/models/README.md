# ONNX/ORT Models Directory

Put your `.onnx` or `.ort` model files in this folder.
For example, you can name your model `model.onnx` or `classifier.ort`.

The Flutter application is configured to look in this directory (`assets/models/`) to load the models for offline local inference.
